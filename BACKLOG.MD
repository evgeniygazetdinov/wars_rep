# Backlog Chat Volc

Что добавить, чтобы приложение перестало быть сырым прототипом. Два репозитория: `chat_volc_backend` + `chat_volc_flutter`.

Идти сверху вниз: без слоя 1 это всё равно учебный стенд.

---

## Слой 1 — чтобы перестало выглядеть как демо

### 1. Закрыть API токеном

JWT сейчас проверяется только на `GET /auth/me`. Поиск, чаты и сообщения может дергать кто угодно, зная `uid`.

- Повесить `get_current_user` на `/users`, `/private_chat`, сообщения.
- Отдавать чат и сообщения только участникам.
- При отправке сообщения автора брать из JWT, не доверять `user_id` с клиента.

### 2. Профиль и аватарки

`avatar_url` уже приходит с Яндекса/ВК, в UI везде буква в кружке.

- Показать фото в списке чатов, поиске и шапке переписки.
- Экран «Я»: имя, email, провайдер, выход.
- Сейчас выход спрятан иконкой без контекста.

### 3. Время и превью как в мессенджере

У сообщения есть `created_at`, в пузыре его нет.

- Время у пузыря.
- В списке чатов: «только что / 5 мин / вчера».
- Разделители дней в ленте.
- Превью обычного текста в списке чатов — с тем же вниманием, что уже есть у стикеров.

### 4. Удаление чата

На бэкенде `DELETE /private_chat/{id}` уже есть, в клиенте нет.

- Свайп или долгое нажатие в списке чатов.
- Подтверждение перед удалением.

### 5. Убрать черновой вход с продакшен-экрана

«Войти по email (тест)» и чипы Яндекс/ВК на одном экране с OAuth.

- Dev-форму спрятать за debug-флаг / `#`.
- Снаружи: Яндекс, ВК и нормальный email/пароль.
- Экраны регистрации и логина по паролю: `/auth/register` и `/auth/login` на бэкенде уже есть.

### 6. Добить локаль

RU/EN переключается, но часть строк зашита по-русски.

- Вынести в `app_strings.dart`: «Смайлы», «Стикеры», «Тестовый вход», «Повторить», диалог OAuth («Завершение входа», hint про URL).

---

## Слой 2 — чтобы жило как чат, а не как форма

### 7. Живая доставка

Сейчас опрос ленты раз в 3 секунды.

- WebSocket или SSE вместо polling.
- Статус сообщения: отправляется / доставлено.
- Redis в compose уже есть — подходит под pub/sub.

### 8. Непрочитанные

- Счётчик на чате.
- Жирное превью непрочитанного.
- Помечать прочитанным при открытии чата (нужно поле/таблица на бэкенде).

### 9. Сообщения: статус и жесты

- Долгое нажатие → копировать / удалить у себя (`DELETE` сообщения на API уже есть).
- Пустой чат: «Напишите первое сообщение {имя}», не голый фон.
- Ошибка сети в шапке открытого чата, не только на списке.

### 10. Поиск без дрожи

Сейчас запрос на каждую букву.

- Debounce ~300 ms.
- Индикатор загрузки (уже есть тонкий прогресс — оставить).
- Если поле пустое — «недавние» собеседники, не пустой экран.

---

## Слой 3 — продукт v2 (не начинать, пока нет слоя 1)

- Тёмная тема.
- Картинки / вложения в чат.
- Пуш-уведомления.
- Онлайн / «печатает».
- Групповые чаты.
- Google OAuth: в схеме `provider` есть, в `resolve_oauth_profile` — нет.

---

## Мелочи бэкенда (дёшево)

- `/healf_check` → `/health` (и поправить healthcheck в Docker).
- Убрать или закрыть авторизацией: `POST /users/`, `GET /users/last_five_users`, `DELETE /users/{id}`.
- `GET /users/{user_id}` ищет по числовому `id`, клиент везде работает с `uid` — выровнять.

---

## Порядок работ

1. Авторизация на API.
2. Аватарки / профиль / время.
3. Удаление чата.
4. Нормальный логин (email+пароль), dev спрятать.
5. Локаль.
6. WebSocket + непрочитанные.
7. Жесты сообщений и поиск.

После пунктов 1–6 Chat Volc читается как тонкий мессенджер, а не как лабораторная работа.
