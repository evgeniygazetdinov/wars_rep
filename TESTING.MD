# План ручного тестирования Chat Volc

Полный проход **backend + Flutter** с нуля: запуск → вход → поиск → чат → переписка в две стороны → выход.

Два репозитория:

| Часть | Путь | Роль |
|---|---|---|
| API | `chat_volc_backend` | FastAPI :8080, JWT, пользователи, приватные чаты, сообщения |
| Клиент | `chat_volc_flutter` | Экраны входа, списка чатов, поиска, переписки |

**Базовый URL API:** `http://127.0.0.1:8080`  
**Swagger:** `http://127.0.0.1:8080/docs`  
Health-эндпоинт назван с опечаткой: **`/healf_check`**.

Для сценариев с двумя людьми нужны **два клиента** (два окна Linux / эмулятор + desktop / инкогнито не поможет — это нативное приложение). Dev-вход по email для этого удобнее OAuth.

---

## 0. Перед стартом

| Что | Как проверить |
|---|---|
| Backend | `./docker.sh start` или `./run.sh` в `chat_volc_backend` |
| БД | Docker: Postgres :5432; локально без `DATABASE_URL` — SQLite `./test.db` |
| `.env` | `JWT_SECRET`, `ALLOW_DEV_AUTH=1` (для тестового входа) |
| Flutter | `flutter pub get` в `chat_volc_flutter` |
| API URL в клиенте | Linux: `--dart-define=API_BASE_URL=http://127.0.0.1:8080` |
| Android-эмулятор | `--dart-define=API_BASE_URL=http://10.0.2.2:8080` |
| OAuth (опционально) | `--dart-define=YANDEX_CLIENT_ID=...` и/или `VK_CLIENT_ID=...` |

Запуск backend:

```bash
cd /home/ev/my_code/chat_volc_backend
cp -n .env.example .env
./docker.sh start
# или локально:
# ./migrate.sh && ./run.sh
```

Запуск Flutter (Linux):

```bash
cd /home/ev/my_code/chat_volc_flutter
flutter pub get
flutter run -d linux --dart-define=API_BASE_URL=http://127.0.0.1:8080
```

Второе окно клиента (другой пользователь):

```bash
flutter run -d linux --dart-define=API_BASE_URL=http://127.0.0.1:8080
```

Если OAuth не настроен — это нормально: в UI есть «Войти по email (тест)».

---

## 1. Дым: API живой

1. `GET http://127.0.0.1:8080/healf_check` → `{"message":"alive"}`.
2. `http://127.0.0.1:8080/docs` — Swagger: Auth, Users, PrivateChats, messages, Health.
3. `http://127.0.0.1:8080/redoc` открывается.
4. `GET /openapi.json` — 200.

**Ожидание:** без 500 в логах, CORS не мешает (разрешены `*`).

---

## 2. Backend: авторизация (Swagger / curl)

Клиент в основном использует `/auth/dev` и `/auth/oauth`. Регистрация и пароль есть только в API.

### 2.1 Dev-вход (`ALLOW_DEV_AUTH=1`)

| # | Шаг | Ожидание |
|---|---|---|
| 2.1.1 | `POST /auth/dev` `{"email":"alice@test.ru","username":"Алиса","provider":"yandex"}` | 200, `access_token`, `token_type: bearer`, `user.uid` / `email` / `username` |
| 2.1.2 | Повтор с тем же email | Тот же пользователь (upsert), не дубль |
| 2.1.3 | `GET /auth/me` с `Authorization: Bearer <token>` | 200, тот же профиль |
| 2.1.4 | `GET /auth/me` без заголовка | **401** `Not authenticated` |
| 2.1.5 | Битый токен | **401** `Invalid or expired token` |
| 2.1.6 | Email без `@` | **400** `Invalid email` |
| 2.1.7 | `ALLOW_DEV_AUTH=0` | **403** `Dev auth is disabled` |

Создать второго пользователя тем же способом: `bob@test.ru` / `Боб`.

### 2.2 Регистрация и пароль (только API)

| # | Шаг | Ожидание |
|---|---|---|
| 2.2.1 | `POST /auth/register` email + пароль ≥ 6 + username | 200, JWT + user, `provider: local` |
| 2.2.2 | Повтор того же email | **409** `Email already registered` |
| 2.2.3 | Пароль короче 6 | **422** |
| 2.2.4 | `POST /auth/login` верные данные | 200, JWT |
| 2.2.5 | Неверный пароль | **401** `Неверный email или пароль` |

### 2.3 OAuth Яндекс / ВК (если есть client id)

1. В клиенте открывается браузер (`oauth.yandex.ru` или `oauth.vk.com`).
2. После редиректа вставить URL / `access_token` в диалог.
3. `POST /auth/oauth` `{"provider":"yandex"|"vk","access_token":"..."}` → JWT + профиль.
4. Повторный вход — тот же `uid`.
5. Невалидный токен → **401**.
6. `provider: google` в схеме есть, бэкенд его не резолвит → **400** `Unsupported provider`.

---

## 3. Backend: пользователи и поиск

| # | Шаг | Ожидание |
|---|---|---|
| 3.1 | `GET /users/search?q=Алис` | 200, Алиса в списке |
| 3.2 | `q` по части email (`alice@`) | Находит по email |
| 3.3 | `exclude_uid=<uid Алисы>` | Алисы нет в выдаче |
| 3.4 | Пустой / пробельный `q` | пустой `users` или 422 (`min_length=1`) |
| 3.5 | `limit=1` | не больше 1; `limit=51` → **422** |
| 3.6 | `GET /users/{uid}/chats` у нового юзера | `chats: []` |
| 3.7 | Несуществующий uid | **404** `User not found` |

Замечание: поиск и список чатов **не требуют JWT**. Это ограничение текущей версии — зафиксировать, не путать с багом UI.

---

## 4. Backend: приватный чат и сообщения

Нужны uid Алисы и Боба.

| # | Шаг | Ожидание |
|---|---|---|
| 4.1 | `POST /private_chat/` `user_one_uid` + `user_two_uid` | 200, `new_chat.id` |
| 4.2 | Повтор той же пары | **409**, в `detail.chat_id` тот же id |
| 4.3 | Пара в обратном порядке | тоже **409**, тот же `chat_id` |
| 4.4 | Чат с самим собой | **400** `cannot create chat with yourself` |
| 4.5 | Несуществующий uid | **404** `one of users not found` |
| 4.6 | `GET /private_chat/{id}` | `user_one_id`, `user_two_id` |
| 4.7 | `GET /private_chat/{id}/all_messages` пустой чат | `[]` |
| 4.8 | `POST /private_chat/{id}/message` `user_id=<uid Алисы>`, `text":"привет"` | 200, `new_message` с `user_uid`, `username`, `created_at` |
| 4.9 | Сообщение от Боба | оба сообщения в `all_messages`, порядок по времени |
| 4.10 | Сообщение от третьего uid | **404** `chat or user not found` |
| 4.11 | Сообщение в несуществующий чат | **404** |
| 4.12 | Тело без `text` | **422** |
| 4.13 | `GET /users/{uid Алисы}/chats` | чат с `peer` = Боб, `last_message` = последнее |
| 4.14 | Список сортируется по `last_message.created_at` (свежий сверху) | да |
| 4.15 | `DELETE /private_chat/{id}/{message_id}` | сообщение исчезло из ленты |
| 4.16 | `DELETE /private_chat/{id}` | чат удалён; повторный GET → 404 |

Клиент **не** умеет удалять чат/сообщение — пункты 4.15–4.16 только через API/Swagger.

---

## 5. Flutter: вход

Запустить клиент при живом API.

| # | Шаг | Ожидание |
|---|---|---|
| 5.1 | Первый запуск | экран «Вход», светлая iOS-тема, «Chat Volc» |
| 5.2 | Переключатель языка RU ↔ EN | заголовки, кнопки, подсказки меняются; выбор сохраняется |
| 5.3 | Яндекс / ВК без CLIENT_ID | ошибка про OAuth, открывается форма тестового входа |
| 5.4 | «Войти по email (тест)» | поля Имя + Email, чипы Яндекс/ВК, в футере URL API |
| 5.5 | Пустые поля / email без `@` | «Укажите имя и корректный email», запроса нет |
| 5.6 | Алиса: имя `Алиса`, email `alice@test.ru` → Продолжить | переход на «Чаты», пустой список «Пока нет чатов…», внизу email |
| 5.7 | Индикатор загрузки на время запроса | кнопка неактивна, повторный клик не шлёт дважды |
| 5.8 | Backend выключен | красная ошибка, не крашится |
| 5.9 | `ALLOW_DEV_AUTH=0` | ошибка с сервера (403), остаёмся на логине |

OAuth (если CLIENT_ID задан):

1. Тап «Войти через Яндекс» / «ВК» → системный браузер.
2. После логина — диалог «Вставьте URL после редиректа или access_token».
3. Отмена → остаёмся на логине.
4. Мусор в поле → «Не удалось найти access_token».
5. Валидный токен → экран чатов, имя/email из провайдера.

---

## 6. Flutter: поиск и создание чата

На клиенте Алисы.

| # | Шаг | Ожидание |
|---|---|---|
| 6.1 | Лупа в AppBar или FAB (карандаш) | экран поиска, фокус в поле |
| 6.2 | Пустое поле | «Введите имя или почту» |
| 6.3 | Набрать `Боб` (Боб уже зарегистрирован во 2-м клиенте или через API) | прогресс-бар, карточка с инициалом, именем, email |
| 6.4 | Поиск по части email | то же |
| 6.5 | Несуществующее имя | «Никого не нашли» |
| 6.6 | Себя в выдаче нет (`exclude_uid`) | Алиса не ищет Алису |
| 6.7 | Тап по Бобу | открывается переписка, в AppBar имя и email Боба |
| 6.8 | Повторный поиск и тап по Бобу | тот же чат (клиент обрабатывает 409 → существующий `chat_id`) |

---

## 7. Flutter: переписка (полный цикл двух пользователей)

Клиент A = Алиса, клиент B = Боб. Оба в одном чате.

| # | Шаг | Ожидание |
|---|---|---|
| 7.1 | Алиса: «привет» + стрелка вверх | пузырь справа, синий; поле очищается |
| 7.2 | Пока отправка | кнопка disabled, спиннер |
| 7.3 | Боб: в течение ~3 с (polling) | сообщение слева, серое, без перезахода |
| 7.4 | Боб отвечает | у Алисы появляется слева, список прокручивается вниз |
| 7.5 | Enter / `textInputAction.send` | отправка |
| 7.6 | Только пробелы | не отправляется |
| 7.7 | Многострочный текст (до 4 строк) | поле растёт |
| 7.8 | Иконка смайла | панель «Смайлы / Стикеры» |
| 7.9 | Смайл | вставляется в текст, не уходит сразу |
| 7.10 | Стикер | панель закрывается, уходит крупный эмодзи (~64px), в API текст вида `[[sticker:🔥]]` |
| 7.11 | В списке чатов превью стикера | «Стикер 🔥», не сырой `[[sticker:…]]` |
| 7.12 | Назад к списку чатов | чат с Бобом, превью = последнее сообщение, ellipsis если длинное |
| 7.13 | Pull-to-refresh | список обновляется |
| 7.14 | Открыть чат снова | вся история на месте, свои справа / чужие слева |
| 7.15 | Тап по фону ленты | клавиатура и панель смайлов закрываются |

---

## 8. Сессия, выход, повторный вход

| # | Шаг | Ожидание |
|---|---|---|
| 8.1 | Полностью закрыть приложение Алисы и открыть снова | сразу «Чаты», не логин (JWT в SharedPreferences) |
| 8.2 | Иконка выхода | экран входа, токен стёрт |
| 8.3 | Снова dev-вход тем же email | те же чаты и история |
| 8.4 | Backend недоступен при старте с сохранённой сессией | сессия сбрасывается (проверка `/auth/me` не прошла) → логин |

---

## 9. Несколько чатов и сортировка

1. Третий пользователь «Вика» (`vika@test.ru`).
2. Алиса пишет Вике, затем снова Бобу.
3. В списке Алисы сверху чат с Бобом (свежее `last_message`).
4. У Вики в списке только чат с Алисой — чата Алиса↔Боб нет.

---

## 10. Ошибки и границы (клиент + API)

| # | Условие | Ожидание |
|---|---|---|
| 10.1 | Остановить backend на экране чатов, pull-to-refresh | красный текст + «Сервер недоступен…» + URL + «Повторить» |
| 10.2 | Поднять backend, «Повторить» | список восстанавливается |
| 10.3 | Двойной тап «Отправить» | одно сообщение |
| 10.4 | Длинное сообщение | пузырь не шире ~78% экрана, текст переносится |
| 10.5 | `POST /private_chat/` без полей | **422** |
| 10.6 | Удалить чат через API, пока он открыт в UI | polling покажет ошибку / пустую ленту, приложение не падает |

---

## 11. UI / локаль / платформы

- Светлая тема Apple: серый grouped background, синие свои пузыри.
- Переходы `CupertinoPageRoute`.
- Язык RU/EN на логине и в чатах; часть строк панели смайлов сейчас только на русском («Смайлы», «Стикеры», «Тестовый вход») — зафиксировать.
- Linux desktop — основной ручной прогон.
- Android-эмулятор: `API_BASE_URL=http://10.0.2.2:8080`, те же сценарии 5–8.
- Узкое окно: поле ввода и пузыри не обрезаны системной клавиатурой (`SafeArea`).

---

## 12. Что в этом проходе не считать продуктовой фичей UI

- Удаление чата и сообщения — только API.
- `POST /auth/register` и `/auth/login` по паролю — нет экрана в Flutter.
- `POST /users/`, `GET /users/last_five_users`, `DELETE /users/{id}` — служебные, клиент не вызывает.
- Redis в compose поднят, на чат не влияет.
- JWT на бэкенде обязателен только для `/auth/me`; чаты и сообщения по uid доступны без токена. Для ручного теста «как пользователь» ходить через клиент; дыру API отметить отдельно, не как шаг счастливого пути.

---

## Рекомендуемый порядок «с начала до конца»

1. Поднять API → `/healf_check` + Swagger.
2. `POST /auth/dev` для Алисы и Боба → `/auth/me` с токеном.
3. Flutter: вход Алисы по email → пустые чаты.
4. Поиск Боба → открыть чат → «привет».
5. Второй клиент: вход Боба → чат в списке → ответ.
6. У Алисы за ~3 с появляется ответ; стикер; превью в списке.
7. Перезапуск приложения Алисы — сессия и история живы.
8. Выход → вход тем же email — чаты на месте.
9. (Опционально) Яндекс/ВК OAuth.
10. (Опционально) Swagger: 409 повтор чата, 400 чат с собой, удаление сообщения/чата.
11. Backend off → ошибка в UI → «Повторить» после включения.

Чеклист двух пользователей для записи результата:

| Пользователь | Email | Клиент | Вход | Чат найден | Сообщение ушло | Сообщение пришло |
|---|---|---|---|---|---|---|
| Алиса | alice@test.ru | окно 1 | ☐ | ☐ | ☐ | ☐ |
| Боб | bob@test.ru | окно 2 | ☐ | ☐ | ☐ | ☐ |
